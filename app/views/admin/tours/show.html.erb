<p id="notice"><%= notice %></p>
<%= @tour.title %> </br>
<%= @tour.description %> </br>
<%= @tour.price %> </br>
<td><%= image_tag(@tour.image, class: "image") if @tour.image.attached?%> </td>
<%= @tour.status.humanize %>
<% if policy(@tour).update? %>
<%= link_to 'Edit', edit_admin_tour_path(@tour) %>
<% end %>
<%= link_to 'Back', admin_tours_path %>
